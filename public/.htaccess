Options -MultiViews
RewriteEngine On

# Serve real files directly (JS, CSS, images, etc.)
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Everything else (including paths that match real directories) → React app
RewriteRule ^ /index.html [L]
